\section{Implementation and Reproducibility}
\label{app:implementation-reproducibility}

This appendix records the repository materials needed to understand and reproduce the computational workflow. All paths are relative to the repository root. The archived outputs and manifests permit verification of the reported evidence, but the material is not described as bit-for-bit reproducible across arbitrary systems.

\subsection{Preserved Source and Extension Architecture}

The authors' source code remains unmodified in \path{Empirical_work/}, \path{example/}, and \path{simulation/}. Thesis-specific code is separated into \path{thesis_extension/src/}, with the frozen design in \path{thesis_extension/config/extension_config.R}. The wrapper files \path{author_oracle_wrapper.R}, \path{author_full_wrapper.R}, and \path{author_bc_dml_wrapper.R} reproduce the estimator logic used by the extension without modifying the preserved source directories. Main outputs are stored by sample size under \path{thesis_extension/final/output/n500/} and \path{thesis_extension/final/output/n1000/}. Diagnostic and reporting code occupy separate namespaces under \path{thesis_extension/diagnostics/} and \path{thesis_extension/reporting/}.

\subsection{Computational Environment}

The canonical runtime is R~3.4.3 dated 30 November 2017 on 64-bit Windows, and the simulations use five workers. The complete captured session is in \path{thesis_extension/environment/sessionInfo_R343.txt}, while \path{thesis_extension/environment/library_manifest_R343.csv} records the installed library inventory. The attached packages in the captured session are \texttt{doSNOW}~1.0.16, \texttt{snow}~0.4-2, \texttt{iterators}~1.0.8, \texttt{foreach}~1.4.3, \texttt{mvtnorm}~1.0-6, \texttt{hqreg}~1.4, \texttt{hdm}~0.2.0, \texttt{quantreg}~5.34, and \texttt{SparseM}~1.77. The session record also lists the loaded namespace versions, including \texttt{Matrix}~1.2-12, \texttt{MatrixModels}~0.4-1, and \texttt{Formula}~1.2-2.

\subsection{Frozen Simulation Configuration}

The main design uses $n\in\{500,1000\}$, $\tau\in\{0.10,0.25,0.50,0.75,0.90\}$, $\kappa\in\{1,0.50,0.25,0.10\}$, and 500 Monte Carlo replications per design. The estimators are Oracle-GMM, Full-GMM, and DML-IVQR-BC. Power is evaluated at offsets $\Delta\in\{-1,-0.5,-0.25,0.25,0.5,1\}$. The primary candidate grid contains 41 values from $-1$ to $3$ in increments of $0.1$. Inference uses a 95\% confidence level and the 0.95 quantile of a chi-square distribution with two degrees of freedom. The DML quantile-Lasso penalty follows the BC pivotal route with 1,000 simulated draws, scale factor 2, and penalty-tail probability 0.1. These settings are recorded in \path{thesis_extension/config/extension_config.R} and summarized in \path{thesis_extension/reporting/EMPIRICAL_RESULTS_FREEZE.md}.

\subsection{Randomness and Seeds}

The main simulation seed is 675, set in \path{thesis_extension/final/run_final.R} from the frozen extension configuration. The grid-domain diagnostic separately sets seed 675 in \path{thesis_extension/diagnostics/grid_sensitivity/grid_sensitivity_config.R}; its Stage~2 reuses the saved Stage~1 primitives rather than generating replacement samples. The 100-replication instrument-strength calibration uses pilot seed 20260813, recorded in \path{thesis_extension/calibration/instrument_strength_100rep/run_strength_calibration.R}. No distinct seed is asserted for a task unless it is recorded in these repository files.

\subsection{Reproduction Workflow}

This workflow documents how the archived analysis was produced and how it can be regenerated from the repository; it is not a one-command or bit-for-bit reproduction claim. Begin from the repository root with the canonical R~3.4.3 \texttt{Rscript} executable and the preserved package library described above. The reproduction workflow should be run from a clean or deliberately prepared output location. The simulation scripts do not provide a general safeguard against overwriting existing output files, so canonical archived results should be preserved before rerunning the workflow. The frozen manifests record the archived file hashes and can be used to compare newly generated outputs, but they do not constitute a general locking mechanism.

After the environment and output locations have been prepared, the empirical inputs are generated in the following dependency order:
\begin{enumerate}
    \item Run the main simulation through \path{thesis_extension/final/run_final.R} with \path{--sample-size=500} and \path{--execute-final}, followed separately by \path{--sample-size=1000} and \path{--execute-final}.
    \item Generate the reported instrument-strength calibration with \path{thesis_extension/calibration/instrument_strength_100rep/run_strength_calibration.R} and \path{--execute-calibration}.
    \item Run the grid-domain diagnostic through \path{thesis_extension/diagnostics/grid_sensitivity/run_grid_sensitivity.R}, first with \path{--execute-stage1} and then, after checking the prespecified trigger, with \path{--execute-stage2}.
    \item Run the coverage forensic audit through \path{thesis_extension/diagnostics/coverage_audit/run_coverage_audit.R} with \path{--execute-coverage-audit}; this audit reads the completed main-run outputs.
    \item Only after all preceding empirical inputs exist, run \path{thesis_extension/reporting/build_thesis_results.R} with \path{--build-reporting}. The reporting builder reads the main-run, calibration, grid-diagnostic, coverage-audit, validation, and environment records; it does not rerun estimation or simulation.
    \item Review the generated reporting consistency checks and the empirical-input and reporting-output manifests together with the diagnostic provenance records listed below.
\end{enumerate}

For the frozen reporting build, the repository must be checked out at commit \texttt{ac9a9ccad027cde05488abb633e66cc02f87cc46}; the builder verifies this exact \texttt{HEAD} value but does not itself certify that the worktree is clean. All required empirical inputs must already be present. The directories \path{thesis_extension/reporting/output/} and \path{thesis_extension/reporting/figures/} must be empty or deliberately prepared as empty, and \path{EMPIRICAL_RESULTS_MANIFEST.csv} and \path{REPORTING_OUTPUT_MANIFEST.csv} must be absent from the reporting directory, because the builder refuses to replace nonempty reporting directories or existing reporting manifests.

\subsection{Failure Handling}

The canonical main-run summaries record zero numerical failures, as confirmed by \path{thesis_extension/reporting/output/reporting_consistency_checks.csv}. The grid-domain diagnostic records one Stage~1 failure: replication 35, $\tau=0.90$, $\kappa=0.10$, DML-IVQR-BC, and candidate $a=-2.1$, with the message ``\texttt{Error info = 95 in stepy2: singular design}.'' Stage~2 records no new evaluation failure. Candidate evaluation is wrapped in error handling: an error or nonfinite statistic is stored with its replication, design, estimator, candidate, stage, and message; the affected statistic is set to missing. A profile containing a failed evaluation is flagged as incomplete, and its aggregate accepted-set measures are reported as missing rather than being computed from a partial profile.

\subsection{Theory and Code Correspondence}

The preserved DML implementation estimates nuisance quantities and evaluates scores on the same sample; it does not use sample splitting or cross-fitting. It uses the BC pivotal penalty and the fitted-normal density proxy documented in Section~3.3. The literal density call is \texttt{dnorm(e, mean(e), var(e))}, so the residual sample variance is supplied to the argument that R interprets as a standard deviation. Instrument residualization regresses $\sqrt{\widehat f_i}Z_{ji}$ on transformed controls with an ordinary unpenalized intercept. The score subtracts that retained intercept and the fitted slopes from the untransformed instrument, without dividing the intercept by $\sqrt{\widehat f_i}$. As explained in Section~3.3, this intercept is implementation specific and is not the literal sample counterpart of the intercept in the augmented population projection.

\subsection{Provenance and Verification Records}

The empirical-results freeze is identified by commit \texttt{ac9a9ccad027cde05488abb633e66cc02f87cc46}, dated 2 September 2026; the pre-writing integration checkpoint is commit \texttt{911e0861114426b5aad314a6f2b7f0c1f92b3fcd}. The primary verification records are \path{PROJECT_STATUS.md}; \path{thesis_extension/validation/R343_final_audit/FINAL_PRE_RUN_AUDIT.md}; \path{thesis_extension/reporting/EMPIRICAL_RESULTS_FREEZE.md}; the input and output hash inventories \path{EMPIRICAL_RESULTS_MANIFEST.csv} and \path{REPORTING_OUTPUT_MANIFEST.csv} in the reporting directory; \path{thesis_extension/reporting/output/reporting_consistency_checks.csv}; and \path{thesis/figures/THESIS_FIGURE_DATA_CHECK.csv}. The grid diagnostic is documented by \path{GRID_RESULTS_PROVENANCE.md} and \path{GRID_RESULTS_MANIFEST.csv} in its directory, while the coverage check is documented by \path{COVERAGE_AUDIT_REPORT.md} and \path{COVERAGE_AUDIT_MANIFEST.csv}. Together these files record configuration, environment, source roles, output checks, and file hashes used to connect the archived computations to the reported thesis results.
